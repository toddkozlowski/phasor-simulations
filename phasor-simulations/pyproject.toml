[project]
name = "phasor-simulations"
version = "0.1.0"
description = "Python framework for generating publication-quality phasor diagrams"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "matplotlib>=3.5.0",
    "numpy>=1.20.0",
    "scipy>=1.7.0",
]
